NPM_TOKEN=npm_your_automation_token_here

# Email testing configuration
# These email addresses are used by the test script to validate mailos functionality

# Email to send FROM during tests (should be a configured account in mailos)
FROM_EMAIL=

# Email to send TO during tests (can be any valid email address)
TO_EMAIL=
